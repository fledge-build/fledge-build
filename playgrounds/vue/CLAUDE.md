# Vue SPA

This is a minimal Vue app.

## Context

This project lives inside a pnpm workspace monorepo but should be treated as an isolated Vue application. You do not need to be aware of the surrounding monorepo structure unless explicitly asked.

## Package management

`pnpm add` and `pnpm install` work directly from this directory.
